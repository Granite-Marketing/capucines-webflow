// import gsap from 'gsap';
// import { SplitText } from 'gsap/SplitText';
// import SplitType from 'split-type';

// gsap.registerPlugin(SplitText);

export const popup = () => {
  const buttons = document.querySelector('[func="show-room-details"]');
  const popup = document.querySelector('.room-detail_component');
  if (!popup || !buttons) return;
  //   const text = popup.querySelector('');
  //   const splitText = new SplitType('.room-detail_content p', { types: 'lines,words' });
  //   console.log(splitText);

  //   gsap.set(splitText.lines, { overflow: 'hidden' });
  //   gsap.set(splitText.words, { yPercent: 100 });
  //   const tl = gsap.timeline({

  //   buttons.addEventListener('click', () => {
  //     popup.classList.add('active');
  //   });
};
